apiVersion: v1
kind: Service
metadata:
  labels:
    app: feed-api
  name: feed-api
  namespace: default
  selfLink: /api/v1/namespaces/default/services/feed-api
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    run: feed-api
  type: ClusterIP